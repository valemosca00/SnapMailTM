{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000"
  },
  "dependencies": {
    "dompurify": "^3.2.3",
    "dotenv": "^16.4.1"
  }
}
